server:
  port: 8000

spring:
  application:
    name: api-gateway

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eureka
